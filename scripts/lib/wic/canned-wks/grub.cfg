serial --unit=0 --speed=115200 --word=8 --parity=no --stop=1
default='Default Provisioning'
timeout=10

search --set=root --label NIRECOVERY

menuentry 'Default Provisioning' {
	linux /bzImage rootwait restore=provision quiet loglevel=1 ni_silent_provision
	initrd /initrd
}

menuentry 'Verbose Provisioning' {
	linux /bzImage rootwait restore=provision
	initrd /initrd
}

menu background splash.jpg
