# NI Images hierarchy

## includes/
c     | nilrt-image-common.inc  # common rules for all NILRT images
cn    | niconsole-image.inc     # bootable rootfs w/ package management
  i   | nilrt-initramfs.inc     # install nilrt-runmode-initramfs as INITRAMFS
   x  | nilrt-xfce.inc          # install pkggrp-ni-xfce and enable X11
    p | nilrt-proprietary.inc   # define NI-proprietary packages
     l| licenses.inc            # tar open-source licenses

## Images
      | * nilrt-recovery-image.bb        # NILRT installation/recovery ISO
c     | * nilrt-runmode-initramfs.bb
cnixpl| * nilrt-runmode-rootfs.bb
      | * nilrt-runmode-system-image.bb
c   pl| * nilrt-safemode-initramfs.bb
      | * nilrt-safemode-image.bb        # NILRT safemode (firmware) rootfs

## rauc/
*DEPRECATED; do not build*
cn   l| * nilrt-base-image.bb           # consolidated base image for RAUC-enabled targets
c     | * nilrt-initramfs.bb            # nilrt-nxg initramfs for RAUC images
      | * nilrt-dkms-bundle-image.bb    # image containing nilrt x64 runmode w/ dkms for RAUC bundle
      |   * nilrt-dkms-bundle.bb        # RAUC bundle to deploy nilrt x64 runmode w/ dkms
