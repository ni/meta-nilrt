# NI Images hierarchy

Mix-ins:
c     | nilrt-image-common.inc  # common rules for all NILRT images
cn    | niconsole-image.inc     # bootable rootfs w/ package management
  i   | nilrt-initramfs.inc     # install niconsole-initramfs as INITRAMFS
   x  | nilrt-xfce.inc          # install pkggrp-ni-xfce and enable X11
    p | nilrt-proprietary.inc   # define NI-proprietary packages

Images:
c     | * niconsole-initramfs.bb         # NILRT common initramfs
cnixp | * nilrt-dkms-image.bb            # NILRT runmode initramfs
      | * nilrt-runmode-system-image.bb  # NILRT runmode rootfs
c   p | * nilrt-safemode-ramdisk.bb      # NILRT safemode initramfs
      | * nilrt-safemode-image.bb        # NILRT safemode (firmware) rootfs
      | * nilrt-recovery-image.bb        # NILRT installation/recovery ISO

RAUC Bundles: *DEPRECATED; do not build*
cn    | * nilrt-base-image.bb           # consolidated base image for RAUC-enabled targets
c     | * nilrt-initramfs.bb            # nilrt-nxg initramfs for RAUC images
      | * nilrt-dkms-bundle-image.bb    # image containing nilrt x64 runmode w/ dkms for RAUC bundle
      |   * nilrt-dkms-bundle.bb        # RAUC bundle to deploy nilrt x64 runmode w/ dkms

NI Kernel Module System (NIKMS): *DEPRECATED; do not build*
k  | nilrt-nikms.inc     # create a rootfs with the module-tools and module-headers
 s | squashfs-image.inc  # disables features that are not needed in squashfses
----
 s | module-tools.bb     # squashfs of kernel module tools 
 s | module-headers.bb   # squashfs of kernel module headers
